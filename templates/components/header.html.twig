{% block content %}

    <nav class="navbar navbar-expand-lg navbar-light  justify-content-center">
        <a class="navbar-brand" href="{{ url('index_home') }}">
            <img src="{{ asset('img/logoP4.png') }}"  class="d-inline-block align-top" id="logo" style="width: 65px; height: 65px">
        </a>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="" href="{{ url('index_home') }}">{{ 'texte_menu.accueil'|trans }}<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="" href="{{ url('news') }}">{{ 'texte_menu.actualites'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="" href="{{ url('team') }}">{{ 'texte_menu.notreBut'|trans }}</a>
                </li>
                <li class="nav-item">
                    <a class="" href="{{ url('contact') }}">{{ 'texte_menu.contact'|trans }}</a>
                </li>

            </ul>
        </div>

    </nav>

{% endblock %}
