{% block content %}

    <nav class="navbar navbar-expand-lg navbar-light  justify-content-center">
        <a class="navbar-brand" href="#">
            <img src="{{ asset('img/logoP4.png') }}" class="d-inline-block align-top" id="logo" style="width: 65px; height: 65px">
        </a>
        <div class="collapse navbar-collapse justify-content-center" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="" href="{{ url('index_home') }}">Accueil<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="" href="{{ url('news') }}">Actualités</a>
                </li>
                <li class="nav-item">
                    <a class="" href="{{ url('team') }}">Notre but</a>
                </li>
                <li class="nav-item">
                    <a class="" href="{{ url('contact') }}">Nous contacter</a>
                </li>

            </ul>
        </div>

    </nav>

{% endblock %}
