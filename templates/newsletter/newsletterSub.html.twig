{% extends('layout.html.twig') %}

{% block content %}
    <div class="container">
        <div class="row form-group">
            {{ form_start(form) }}
            <div class="row newsletter">{{ form_row(form.Newsletter) }}</div>
            <div class="row envoyer">{{ form_row(form.envoyer) }}</div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
