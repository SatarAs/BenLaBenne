{% extends 'layout.html.twig' %}
{% block page_title %}Edition {{ article.titre }}{% endblock %}

{% block content %}
    <div class="container margin-top">
        <div class="row text-center">
            <h1>Editer l'article</h1>
            <h1 class="rounded mx-auto d-block">" {{ article.titre }} "</h1>
        </div>
    </div>

    <div class="container-fluid">
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <div class="row text-center">
            {{ form_row(form.titre) }}
            {{ form_row(form.chapo) }}
            {{ form_row(form.contenu) }}
        </div>
    </div>

    <button class="btn btn-success btn-sm rounded mx-auto d-block margin-top">{{ button_label|default('Update') }}</button>
            {{ form_end(form) }}

    <a  class="rounded mx-auto d-block" href="{{ path('article_index') }}" style="text-align: center">Retour à la liste</a>

            {{ include('article/_delete_form.html.twig') }}


{% endblock %}
